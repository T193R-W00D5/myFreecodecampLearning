name: E2E Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Node.js with Volta
      uses: volta-cli/action@v4

    - name: Install dependencies
      run: npm ci

    - name: Run unit tests
      run: npm run test:unit

  e2e-tests:
    name: E2E Tests (All Browsers - Comprehensive CI)
    runs-on: ubuntu-latest
    needs: unit-tests

    strategy:
      fail-fast: false
      matrix:
        # Run all desktop browsers for comprehensive CI testing
        browser: [chromium, firefox, webkit, "Microsoft Edge"]
        # browser: [chromium]  # <-- Use this line for faster CI when needed

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Node.js with Volta
      uses: volta-cli/action@v4

    - name: Install dependencies
      run: npm ci

    - name: Install Playwright Browsers
      run: npx playwright install --with-deps ${{ matrix.browser }}

    - name: Run Playwright tests
      run: npx playwright test --project=${{ matrix.browser }}
      env:
        CI: true

    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: playwright-results-${{ matrix.browser }}
        path: |
          test-results/
          playwright-report/
        retention-days: 30

    - name: Upload test report
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: playwright-report-${{ matrix.browser }}
        path: playwright-report/
        retention-days: 30

  e2e-tests-full:
    name: E2E Tests (All Browsers - Main Branch Only)
    runs-on: ubuntu-latest
    needs: unit-tests
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Node.js with Volta
      uses: volta-cli/action@v4

    - name: Install dependencies
      run: npm ci

    - name: Install Playwright Browsers
      run: npx playwright install --with-deps

    - name: Run all Playwright tests
      run: npm run test:e2e
      env:
        CI: true

    - name: Generate test report
      if: always()
      run: npx playwright show-report --host=0.0.0.0 --port=9323 &

    - name: Upload comprehensive test results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: playwright-comprehensive-results
        path: |
          test-results/
          playwright-report/
        retention-days: 90